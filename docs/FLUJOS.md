# Flujos del Sistema - Sistema de Información de Acciones

## 🔄 Flujo de Sincronización de Stocks

```
┌─────────────┐
│   Cliente   │
│  (Frontend) │
└──────┬──────┘
       │
       │ GraphQL Mutation: syncStocks
       ▼
┌─────────────────────┐
│  GraphQL Handler    │
│  (Presentation)     │
└──────┬──────────────┘
       │
       │ Llamada a servicio
       ▼
┌─────────────────────┐
│   SyncService       │
│  (Application)     │
└──────┬──────────────┘
       │
       │ Loop de paginación
       ▼
┌─────────────────────┐
│  KarenAIClient      │
│  (Infrastructure)  │
└──────┬──────────────┘
       │
       │ HTTP GET con paginación
       ▼
┌─────────────────────┐
│  API Externa        │
│  (karenai.click)   │
└──────┬──────────────┘
       │
       │ JSON Response
       ▼
┌─────────────────────┐
│  KarenAIClient      │
│  (convierte DTOs)   │
└──────┬──────────────┘
       │
       │ Entidades de dominio
       ▼
┌─────────────────────┐
│  SyncService        │
│  (batch upsert)    │
└──────┬──────────────┘
       │
       │ Llamada a repositorio
       ▼
┌─────────────────────┐
│ StockRepository     │
│ (Infrastructure)    │
└──────┬──────────────┘
       │
       │ SQL INSERT/UPDATE
       ▼
┌─────────────────────┐
│   CockroachDB       │
└─────────────────────┘
       │
       │ Success
       ▼
┌─────────────────────┐
│  GraphQL Response   │
│  (al cliente)      │
└─────────────────────┘
```

**Pasos detallados**:
1. Cliente envía mutation `syncStocks` vía GraphQL
2. Handler recibe la petición y valida
3. SyncService inicia proceso de sincronización
4. KarenAIClient hace requests paginados a API externa
5. Cada página se convierte a entidades de dominio
6. StockRepository hace batch upsert en CockroachDB
7. Se retorna respuesta de éxito al cliente

---

## 🔍 Flujo de Consulta de Stocks

```
┌─────────────┐
│   Cliente   │
│  (Frontend) │
└──────┬──────┘
       │
       │ GraphQL Query: stocks(filter, sort)
       ▼
┌─────────────────────┐
│  GraphQL Handler    │
│  (Presentation)     │
└──────┬──────────────┘
       │
       │ Resolver
       ▼
┌─────────────────────┐
│  StockService       │
│  (Application)     │
└──────┬──────────────┘
       │
       │ Llamada a repositorio
       ▼
┌─────────────────────┐
│ StockRepository     │
│ (Infrastructure)    │
└──────┬──────────────┘
       │
       │ SQL SELECT con filtros
       ▼
┌─────────────────────┐
│   CockroachDB       │
└──────┬──────────────┘
       │
       │ Resultados
       ▼
┌─────────────────────┐
│ StockRepository     │
│ (convierte a domain)│
└──────┬──────────────┘
       │
       │ Entidades de dominio
       ▼
┌─────────────────────┐
│  StockService       │
│  (convierte a DTOs) │
└──────┬──────────────┘
       │
       │ DTOs
       ▼
┌─────────────────────┐
│  GraphQL Handler    │
│  (convierte a types)│
└──────┬──────────────┘
       │
       │ GraphQL Types
       ▼
┌─────────────────────┐
│  GraphQL Response   │
│  (JSON al cliente)  │
└─────────────────────┘
```

**Optimizaciones**:
- Cache en StockService (TTL configurable)
- Índices en BD para búsquedas rápidas
- DataLoader para evitar N+1 queries

---

## 💡 Flujo de Recomendaciones

```
┌─────────────┐
│   Cliente   │
│  (Frontend) │
└──────┬──────┘
       │
       │ GraphQL Query: recommendations(limit)
       ▼
┌─────────────────────┐
│  GraphQL Handler    │
└──────┬──────────────┘
       │
       │ Resolver
       ▼
┌─────────────────────┐
│ RecommendationService│
│  (Application)      │
└──────┬──────────────┘
       │
       │ 1. Obtener todos los stocks
       ▼
┌─────────────────────┐
│  StockService       │
│  (con filtro rating)│
└──────┬──────────────┘
       │
       │ Stocks filtrados
       ▼
┌─────────────────────┐
│ RecommendationService│
│  (calcular scores)  │
└──────┬──────────────┘
       │
       │ Para cada stock:
       │ - Calcular priceChange
       │ - Calcular ratingScore
       │ - Calcular actionScore
       │ - Calcular finalScore
       ▼
┌─────────────────────┐
│ RecommendationService│
│  (ordenar O(n log n))│
└──────┬──────────────┘
       │
       │ Top N recomendaciones
       ▼
┌─────────────────────┐
│  GraphQL Handler    │
│  (convierte a types)│
└──────┬──────────────┘
       │
       │ GraphQL Response
       ▼
┌─────────────────────┐
│   Cliente           │
│  (recibe top stocks)│
└─────────────────────┘
```

**Algoritmo**:
1. Filtrar stocks con rating positivo
2. Calcular score para cada stock (O(n))
3. Ordenar por score descendente (O(n log n))
4. Retornar top N

---

## 🎨 Flujo Frontend - Búsqueda de Stocks

```
┌─────────────┐
│   Usuario   │
│  (escribe)  │
└──────┬──────┘
       │
       │ Input onChange
       ▼
┌─────────────────────┐
│  SearchInput        │
│  (Component)       │
└──────┬──────────────┘
       │
       │ useDebounce(300ms)
       ▼
┌─────────────────────┐
│  withSearch HOC     │
│  (lógica búsqueda)  │
└──────┬──────────────┘
       │
       │ Verificar cache
       ▼
┌─────────────────────┐
│  useStock Composable│
│  (cache check)      │
└──────┬──────────────┘
       │
       │ Cache miss?
       ▼
┌─────────────────────┐
│  useApi Composable  │
│  (GraphQL client)   │
└──────┬──────────────┘
       │
       │ Request deduplication
       │ GraphQL Query
       ▼
┌─────────────────────┐
│  GraphQL API        │
│  (Backend)          │
└──────┬──────────────┘
       │
       │ Response
       ▼
┌─────────────────────┐
│  useApi Composable  │
│  (guardar en cache) │
└──────┬──────────────┘
       │
       │ Actualizar store
       ▼
┌─────────────────────┐
│  StockStore (Pinia) │
└──────┬──────────────┘
       │
       │ Reactive update
       ▼
┌─────────────────────┐
│  StockTable         │
│  (renderiza)        │
└─────────────────────┘
```

**Optimizaciones**:
- Debounce de 300ms para evitar requests excesivos
- Cache en memoria con TTL
- Request deduplication
- Reactive updates con Vue 3

---

## 🎨 Flujo Frontend - Renderizado de Tabla

```
┌─────────────┐
│  StockList  │
│  (View)     │
└──────┬──────┘
       │
       │ withLoading HOC
       ▼
┌─────────────────────┐
│  Loading State      │
│  (Spinner)          │
└──────┬──────────────┘
       │
       │ Data loaded
       ▼
┌─────────────────────┐
│  withError HOC      │
│  (error handling)   │
└──────┬──────────────┘
       │
       │ No errors
       ▼
┌─────────────────────┐
│  withPagination HOC │
│  (paginación)       │
└──────┬──────────────┘
       │
       │ Paginated data
       ▼
┌─────────────────────┐
│  StockTable         │
│  (Design System)    │
└──────┬──────────────┘
       │
       │ Render items
       ▼
┌─────────────────────┐
│  Virtual Scrolling  │
│  (solo visibles)    │
└──────┬──────────────┘
       │
       │ Render
       ▼
┌─────────────────────┐
│  StockRow           │
│  (por cada stock)   │
└─────────────────────┘
```

**Optimizaciones**:
- Virtual scrolling para listas grandes
- HOCs separan lógica de presentación
- Lazy loading de componentes

---

## 🔄 Flujo de Cambio de Tema

```
┌─────────────┐
│   Usuario   │
│  (click)    │
└──────┬──────┘
       │
       │ Click en ThemeToggle
       ▼
┌─────────────────────┐
│  ThemeToggle        │
│  (Component)        │
└──────┬──────────────┘
       │
       │ Toggle theme
       ▼
┌─────────────────────┐
│  ThemeStore (Pinia) │
│  (state: 'light'|'dark')│
└──────┬──────────────┘
       │
       │ Reactive update
       ▼
┌─────────────────────┐
│  ThemeProvider      │
│  (aplica tokens)    │
└──────┬──────────────┘
       │
       │ CSS Variables
       ▼
┌─────────────────────┐
│  Todos los componentes│
│  (actualizan colores)│
└─────────────────────┘
```

**Implementación**:
- Store de Pinia para estado global
- CSS Variables para tokens de color
- Transición suave entre temas

---

## 📊 Flujo de Carga Inicial

```
┌─────────────┐
│   Usuario   │
│  (abre app) │
└──────┬──────┘
       │
       │ Navegación
       ▼
┌─────────────────────┐
│  Router (Vue)       │
│  (lazy load route)  │
└──────┬──────────────┘
       │
       │ Carga componente
       ▼
┌─────────────────────┐
│  StockList          │
│  (onMounted)        │
└──────┬──────────────┘
       │
       │ Fetch data
       ▼
┌─────────────────────┐
│  useStock Composable│
│  (check cache)      │
└──────┬──────────────┘
       │
       │ Cache miss
       ▼
┌─────────────────────┐
│  GraphQL Query      │
│  (stocks)           │
└──────┬──────────────┘
       │
       │ Response
       ▼
┌─────────────────────┐
│  Update Store       │
│  (Pinia)            │
└──────┬──────────────┘
       │
       │ Reactive render
       ▼
┌─────────────────────┐
│  UI Actualizada     │
└─────────────────────┘
```

**Optimizaciones**:
- Code splitting por ruta
- Cache en store
- Lazy loading de componentes

---

## 🔐 Flujo de Autenticación (Futuro)

```
┌─────────────┐
│   Usuario   │
│  (login)    │
└──────┬──────┘
       │
       │ Credentials
       ▼
┌─────────────────────┐
│  AuthService        │
│  (Frontend)         │
└──────┬──────────────┘
       │
       │ GraphQL Mutation
       ▼
┌─────────────────────┐
│  GraphQL Handler    │
│  (login)            │
└──────┬──────────────┘
       │
       │ Validate
       ▼
┌─────────────────────┐
│  AuthService        │
│  (Backend)          │
└──────┬──────────────┘
       │
       │ JWT Token
       ▼
┌─────────────────────┐
│  Store Token        │
│  (localStorage)     │
└──────┬──────────────┘
       │
       │ Include in headers
       ▼
┌─────────────────────┐
│  GraphQL Client     │
│  (future requests)  │
└─────────────────────┘
```

**Nota**: Este flujo es para futuras implementaciones. Actualmente la API externa usa Bearer token fijo.

---

**Última actualización**: [Fecha]
