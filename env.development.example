# ============================================
# Variables de Entorno - Desarrollo
# ============================================
# Este archivo contiene las variables de entorno para desarrollo
# tanto del backend (Go) como del frontend (Vue)
#
# INSTRUCCIONES:
# 1. Copia este archivo a .env.development en la raíz del workspace
# 2. Copia las variables del backend a api/.env.development
# 3. Copia las variables del frontend a frontend/.env.development
# 4. Actualiza los valores según tu entorno

# ============================================
# BACKEND - Go API
# ============================================

# Base de Datos (CockroachDB)
DB_HOST=localhost
DB_PORT=26257
DB_USER=root
DB_PASSWORD=
DB_NAME=stocks
DB_SSLMODE=disable

# API Externa (KarenAI)
API_BASE_URL=https://api.karenai.click
API_KEY=tu_api_key_aqui

# Servidor Backend
PORT=8080

# ============================================
# FRONTEND - Vue 3
# ============================================

# Endpoint GraphQL del backend
VITE_GRAPHQL_ENDPOINT=http://localhost:8080/query

# ============================================
# NOTAS
# ============================================
# 
# Backend (Go):
# - Las variables sin prefijo son para Go
# - Para cargar: export $(cat .env.development | grep -v '^#' | xargs)
# - O usar un paquete como github.com/joho/godotenv
#
# Frontend (Vue/Vite):
# - Las variables con prefijo VITE_ son expuestas al cliente
# - Vite solo expone variables que empiezan con VITE_
# - Vite carga automáticamente .env.development si está en frontend/
#
# Para usar este archivo:
# 1. Backend: Copiar variables relevantes a api/.env.development
# 2. Frontend: Copiar variables relevantes a frontend/.env.development
# 3. O usar una herramienta como direnv para cargar automáticamente
