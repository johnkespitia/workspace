# ============================================
# Variables de Entorno - Frontend (Vue) - Desarrollo
# ============================================
# Copia este archivo a .env.development
# Nota: Vite solo expone variables que empiezan con VITE_

# Endpoint GraphQL del backend
VITE_GRAPHQL_ENDPOINT=http://localhost:8080/query

# ============================================
# NOTAS
# ============================================
# Vite carga automáticamente archivos .env en el directorio frontend/
# - .env.development se carga en modo desarrollo (npm run dev)
# - .env.production se carga en modo producción (npm run build)
# - .env.local se carga siempre (no debe commitearse)
#
# Las variables deben empezar con VITE_ para ser expuestas al cliente
# Acceso en código: import.meta.env.VITE_GRAPHQL_ENDPOINT
